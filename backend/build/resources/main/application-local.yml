spring:
  application:
    name: prepaid-platform
  
  datasource:
    url: jdbc:postgresql://localhost:5433/prepaid_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul
  
  flyway:
    enabled: true
    baseline-on-migrate: true

  data:
    redis:
      host: localhost
      port: 6380



  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: ${NAVER_CLIENT_ID}
            client-secret: ${NAVER_CLIENT_SECRET}
            redirect-uri: http://localhost:8080/login/oauth2/code/naver
            authorization-grant-type: authorization_code
            scope: name,email
            client-name: Naver
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

  kafka:
    bootstrap-servers: localhost:29092
    consumer:
      group-id: prepaid-group
      auto-offset-reset: earliest
  
  jackson:
    time-zone: Asia/Seoul

  threads:
    virtual:
      enabled: true

toss:
  client-key: ${TOSS_CLIENT_KEY}
  secret-key: ${TOSS_SECRET_KEY}
  url: https://api.tosspayments.com/v1/payments

jwt:
  secret: c3ByaW5nLWJvb3Qtc2VjdXJpdHktand0LXNlY3JldC1rZXktZm9yLXRva2VuLWdlbmVyYXRpb24td2l0aC01MTItYml0cy1hbGdvcml0aG0tc2lnbmF0dXJl
  expiration: 1800000 # 30 mins
  refresh-token:
    expiration: 604800000 # 7 days

logging:
  level:
    org.springframework.web: INFO
    org.hibernate.SQL: INFO
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
